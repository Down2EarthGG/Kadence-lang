note: Environment variable utilities

let processMod = run require "process"

export function get name
    return processMod.env[name]
end

export function has name
    return (get name) is not undefined
end
